# How to configure your Flask Heroku app to answer ACME challenges using
# certbot-heroku

- __demoproject/main.py__ simple app that reads the `LETS_ENCRYPT_CHALLENGE`
  environment variable (Heroku config var), and outputs it.
